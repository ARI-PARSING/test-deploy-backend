import { fileExists, readFileJson, readFileTxt, readFileXml, removeFile } from '../utils/fileHandler.util.js';
import { mapDataToListMap } from '../utils/mappingData.util.js';

const fileConverter = async (filePath, fileType) => {
};

export {
    fileConverter
}